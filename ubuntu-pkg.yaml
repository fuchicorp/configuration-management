---
- name: Installing pkg Ubuntu only
  hosts: all
  connection: local
  user: ubuntu
  gather_facts: no 
  tasks:
    - name: install apache on ubuntu systems
      when: ansible_distribution == "Ubuntu" and ansible_distribution_major_version == "16"
      apt:
        name: apache2
        state: present


